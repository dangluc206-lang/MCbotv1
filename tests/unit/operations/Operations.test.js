'use strict';
const test=require('node:test');const assert=require('node:assert/strict');const Operation=require('../../../src/operations/Operation');const OperationManager=require('../../../src/operations/OperationManager');const OperationQueue=require('../../../src/operations/OperationQueue');const OperationLockPolicy=require('../../../src/operations/OperationLockPolicy');const OperationTimeoutPolicy=require('../../../src/operations/OperationTimeoutPolicy');
function manager(){return new OperationManager({botId:'a',queue:new OperationQueue(),lockPolicy:new OperationLockPolicy(),timeoutPolicy:new OperationTimeoutPolicy()});}
test('operation succeeds and releases lock',async()=>{const m=manager();const result=await m.run(new Operation({name:'x',lockKeys:['gui'],execute:async()=>5}),{timeoutMs:50});assert.equal(result.success,true);assert.equal(result.data,5);assert.equal(m.lockPolicy.owner('gui'),null);});
test('operation timeout releases lock',async()=>{const m=manager();const result=await m.run(new Operation({name:'x',lockKeys:['gui'],execute:()=>new Promise(()=>{})}),{timeoutMs:5});assert.equal(result.success,false);assert.equal(result.status,'TIMEOUT');assert.equal(m.lockPolicy.owner('gui'),null);});
