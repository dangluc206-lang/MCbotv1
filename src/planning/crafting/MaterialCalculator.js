'use strict';
class MaterialCalculator{constructor({recipeRegistry}){this.recipeRegistry=recipeRegistry;}requirements(outputId,amount,path=new Set()){const recipe=this.recipeRegistry.ids().map(id=>[id,this.recipeRegistry.require(id)]).find(([,r])=>r.output===outputId);if(!recipe)return{[outputId]:amount};const [recipeId,definition]=recipe;if(path.has(recipeId))throw new Error(`Recipe cycle detected: ${recipeId}`);path.add(recipeId);const crafts=Math.ceil(amount/(definition.outputAmount||1));const totals={};for(const [inputId,inputAmount] of Object.entries(definition.inputs||{})){const nested=this.requirements(inputId,inputAmount*crafts,new Set(path));for(const [id,count] of Object.entries(nested))totals[id]=(totals[id]||0)+count;}return totals;}}
module.exports=MaterialCalculator;
